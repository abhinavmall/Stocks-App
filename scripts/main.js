var React = require('react');
var ReactDOM = require('react-dom');

var StockChart = React.createClass({
    render: function() {
        return (
            <p>AbhinavMall</p>
        )
    }
});

ReactDOM.render(<StockChart/>, document.querySelector('#main'));
